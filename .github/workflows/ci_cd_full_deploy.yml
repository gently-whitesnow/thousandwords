#-----------------------------------------------------------------------------------------
#
# Сборка и деплой проекта thousandwords
# 
#-----------------------------------------------------------------------------------------

name: deploy thousandwords

on:
  push:
    branches:
      - 'master'
jobs:

  build_and_deploy_backend:

    runs-on: ubuntu-latest
    
    steps:
      -
        name: Checkout 
        uses: actions/checkout@master
      -
        name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_LOGIN }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      -
        name: Build and push
        run: cd backend
        uses: docker/build-push-action@v3
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_LOGIN }}/thousandwords:latest
  
  # build_and_deploy_static:

  #   runs-on: ubuntu-latest
    
  #   steps:
  #     - uses: actions/checkout@master
  #     - name: Login
  #       run: docker login registry-1.docker.io/v2 -u ${{ secrets.DOCKER_LOGIN }} -p ${{ secrets.DOCKER_PWD }}
  #     - name: build backend
  #       run: |
  #         cd backend
  #         docker build -t gently4005/thousandwords:latest .
  #     - name: push docker image
  #       run: docker push gently4005/thousandwords:latest
